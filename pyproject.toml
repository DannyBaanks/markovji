[project]
name = "markovji"
version = "1.0.0"
description = "Algoritmo de Markov esotérico con sintaxis kaomoji — Turing-completo, unario, reescritura"
readme = "README.md"
license = {text = "MIT"}
requires-python = ">=3.10"
authors = [
    {name = "ISyCo", email = "isyco@example.com"}
]
keywords = ["esolang", "markov", "rewriting", "turing-complete", "kaomoji", "unary"]
classifiers = [
    "Development Status :: 4 - Beta",
    "Environment :: Console",
    "License :: OSI Approved :: MIT License",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.10",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
    "Topic :: Software Development :: Interpreters",
]

[project.scripts]
markovji = "interpreter:main"

[project.urls]
Repository = "https://github.com/ISyCo/markovji"
Issues = "https://github.com/ISyCo/markovji/issues"

[build-system]
requires = ["setuptools>=61.0", "wheel"]
build-backend = "setuptools.build_meta"

[tool.ruff]
line-length = 100
target-version = "py310"

[tool.mypy]
python_version = "3.10"
warn_return_any = true